---
type: routines
site: tekt.observer
updated: 2026-08-01
---

## Scheduled track run (existing)

```yaml
status: active
updated: 2026-08-01
cadence: per .schedule.local (daily/weekly/monthly)
```

Runs `scripts/run_track.sh` for each due track. Maps to the phase chain: `/gather` → `/organize` → `/understand` → `/generate`. No manual intervention. See `.knowledge/architecture.md` sequence diagram.

---

## Source health check (planned)

```yaml
status: idea
updated: 2026-08-01
cadence: weekly
```

For each connected source, run `/seek` with a canary query to verify the sensor is still functional. Report degraded sources via the configured delivery channel. This replaces ad-hoc canary checks embedded in the integration loop with a proactive, scheduled health monitor.
