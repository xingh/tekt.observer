# Completed work archive

Completed task and plan documents live under `.archive/<YYYY>/` and end in
`.completed.md`. Active work belongs in `.manage/`; durable documentation
belongs in `.knowledge/`; product and regeneration intent belongs in
`.arkitype/`.

Preserve the original date and descriptive filename when archiving, update
inbound links, and remove the corresponding active entry from `.manage/`.
